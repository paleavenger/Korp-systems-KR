<Window x:Class="KR2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Анализатор сетевых подключений" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="150"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Сетевые интерфейсы:" FontWeight="Bold" Margin="0,0,0,4"/>

            <ListBox Grid.Row="1" x:Name="InterfaceListBox"
                     SelectionChanged="InterfaceListBox_SelectionChanged"/>

            <Button Grid.Row="2" Content="Обновить список"
                    Margin="0,4,0,8" Padding="4"
                    Click="RefreshInterfaces_Click"/>

            <GroupBox Grid.Row="3" Header="Информация об интерфейсе">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="InterfaceInfoText"
                               TextWrapping="Wrap" FontSize="11"
                               Text="Выберите интерфейс..."/>
                </ScrollViewer>
            </GroupBox>

            <TextBlock Grid.Row="4" Text="История URL:" FontWeight="Bold" Margin="0,8,0,4"/>
            <ListBox Grid.Row="5" x:Name="HistoryListBox"
                     SelectionChanged="HistoryListBox_SelectionChanged"/>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Header="Анализ URL" Margin="0,0,0,8">
                <Grid Margin="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="0" x:Name="UrlInputBox"
                             Padding="4" Margin="0,0,0,6"
                             Text="https://example.com/path?param=value#fragment"
                             KeyDown="UrlInputBox_KeyDown"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        <Button Content="Разобрать URL" Padding="8,4" Margin="0,0,6,0"
                                Click="AnalyzeUrl_Click"/>
                        <Button Content="Ping" Padding="8,4" Margin="0,0,6,0"
                                Click="PingHost_Click"/>
                        <Button Content="DNS" Padding="8,4"
                                Click="DnsLookup_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
            <GroupBox Grid.Row="1" Header="Компоненты URL" Margin="0,0,0,8">
                <Grid Margin="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Схема:" Margin="0,2"/>
                    <TextBox   Grid.Row="0" Grid.Column="1" x:Name="TbScheme"   IsReadOnly="True" Margin="0,2,8,2" Padding="2"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Хост:" Margin="0,2"/>
                    <TextBox   Grid.Row="0" Grid.Column="3" x:Name="TbHost"     IsReadOnly="True" Margin="0,2" Padding="2"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Порт:" Margin="0,2"/>
                    <TextBox   Grid.Row="1" Grid.Column="1" x:Name="TbPort"     IsReadOnly="True" Margin="0,2,8,2" Padding="2"/>
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Путь:" Margin="0,2"/>
                    <TextBox   Grid.Row="1" Grid.Column="3" x:Name="TbPath"     IsReadOnly="True" Margin="0,2" Padding="2"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Параметры:" Margin="0,2"/>
                    <TextBox   Grid.Row="2" Grid.Column="1" x:Name="TbQuery"    IsReadOnly="True" Margin="0,2,8,2" Padding="2"/>
                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Фрагмент:" Margin="0,2"/>
                    <TextBox   Grid.Row="2" Grid.Column="3" x:Name="TbFragment" IsReadOnly="True" Margin="0,2" Padding="2"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Тип адреса:" Margin="0,2"/>
                    <TextBox   Grid.Row="3" Grid.Column="1" x:Name="TbAddrType" IsReadOnly="True" Margin="0,2,8,2" Padding="2"/>
                    <Button    Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2"
                               Content="Очистить историю" Padding="4,2" Margin="0,2"
                               Click="ClearHistory_Click"/>
                </Grid>
            </GroupBox>
            <GroupBox Grid.Row="3" Header="Результаты диагностики">
                <TextBox x:Name="DiagnosticsOutput"
                         IsReadOnly="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="11"
                         Text="Здесь будут результаты..."/>
            </GroupBox>
        </Grid>
    </Grid>
</Window>